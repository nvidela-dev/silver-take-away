// Deterministic demo data seed — implemented in PR-2.
// Populates 6-8 users, 10-15 vendors, 8-12 categories, 50+ bills across all
// statuses, payments, and activity logs. See REQUIREMENTS §10.

async function main() {
  console.log("[seed] Not implemented yet — landing in PR-2.");
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
