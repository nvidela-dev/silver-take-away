nodeLinker: node-modules

enableScripts: true

# Yarn 4 disables build scripts by default for security. Re-enable the ones
# we actually need so drizzle-kit, sharp, and Tailwind native bindings work.
supportedArchitectures:
  os:
    - current
  cpu:
    - current
  libc:
    - current
